

  #type help("keywords") command on python shell/terminal
  #It will show all the keywords of python depending on its version.

  #What is keywords in python?
 -special reserved words in python
 -they cannot be used as variable,function,identifiers
 -they must be written in lowercase
 -they trigger special/specific action or tasks
 
 #From Google
  Keywords are reserved words in Python that are used to trigger specific tasks. We cannot use a keyword as a variable name, function name, or any other identifier since these keywords each hold a special meaning. All of the keywords in Python are lowercase.
